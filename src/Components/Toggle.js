//import { useSwitch } from "@mui/base/SwitchUnstyled"; UseSwitch hook from MUI - might be usefull to deal with state of the toggle/switch
import Switch from "@mui/material/Switch";

function Toggle() {
  return (
    <div className="toggle">
      <Switch />
    </div>
  );
}

export default Toggle;
